# MoneyTrail/urls.py
from django.urls import path

# This file can be left nearly empty or contain only specific URLs
# that are meant to be included at a sub-path if your app had more HTML views.
# For this project, the main transaction_tracker/urls.py handles the root HTML
# and the API routing directly.
urlpatterns = [
    # Example for future app-specific HTML views (if needed, otherwise leave empty):
    # path('some-other-page/', views.SomeOtherPageView.as_view(), name='some_other_page'),
]
